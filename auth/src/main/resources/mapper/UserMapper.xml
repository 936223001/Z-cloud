<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zqj.auth.mapper.UserMapper">
    <!-- 通用查询映射结果 -->
    <sql id="Base_Column_List">
        `id`,
        `username`,
        `password`,
        `dept_id`,
        `email`,
        `mobile`,
        `status`,
        `create_time`,
        `update_time`,
        `last_login_time`,
        `ssex`,
        `avatar`,
        `description`
    </sql>
    <resultMap id="BaseResultMap" type="com.zqj.common.entity.sys.User">
        <result column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="dept_id" property="deptId"/>
        <result column="email" property="email"/>
        <result column="mobile" property="mobile"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="ssex" property="ssex"/>
        <result column="avatar" property="avatar"/>
        <result column="description" property="description"/>
    </resultMap>

    <!--auto generated by MybatisCodeHelper on 2021-04-20-->
    <select id="findByUsername" resultType="com.zqj.common.entity.sys.User">
        select u.id                      userId,
               u.username                username,
               u.email                   email,
               u.mobile                  mobile,
               u.password                password,
               u.status                  status,
               u.create_time             createTime,
               u.ssex                    sex,
               u.dept_id                 deptId,
               u.last_login_time         lastLoginTime,
               u.update_time             modifyTime,
               u.description             description,
               u.avatar                  avatar,
               d.dept_name               deptName,
               group_concat(r.id)        roleId,
               group_concat(r.role_name) roleName
        from sys_user u
                     left join sys_dept d on (u.dept_id = d.id)
                     left join sys_user_role ur on (u.id = ur.user_id)
                     left join sys_role r on r.id = ur.role_id
        <where>
            <if test="username != null and username != ''">
                and u.username = #{username}
            </if>
        </where>
        group by u.username, u.id
    </select>
</mapper>
